{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "VersionPublish executor",
  "description": "",
  "type": "object",
  "properties": {
    "specifier": {
      "type": "string",
      "description": "The version specifier to publish."
    },
    "buildPath": {
      "type": "string",
      "description": "The path to the build output directory.",
      "default": "dist"
    }
  },
  "required": ["specifier", "buildPath"]
}
